'use client';

import { CasePage, CasePageProps } from '@mediature/main/src/app/(private)/dashboard/authority/[authorityId]/case/sss/CasePage';

export default function Page(props: CasePageProps) {
  return <CasePage {...props} />;
}
